pluginManagement {
    repositories {
        maven {
            url "${NEXUS_GRADLE_PLUGINS_URL}"
            credentials {
                username "${NEXUS_USER}"
                password "${NEXUS_PASSWORD}"
            }
        }
    }
}

rootProject.name = 'template'


